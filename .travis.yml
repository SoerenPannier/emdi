# R for travis: see documentation at https://docs.travis-ci.com/user/languages/r
before_install:
  - sudo apt-get update
language: R
sudo: false
cache: packages
r_github_packages: 
 - cran/survival
addons:
  apt:
    packages:
      - libudunits2-de
